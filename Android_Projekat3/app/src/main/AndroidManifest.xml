<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projekat3">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".application.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projekat3">

        <activity
            android:name=".presentation.view.activities.AppActivity"
            android:exported="true"/>
        <activity
            android:name=".presentation.view.activities.SellActivity"
            android:exported="true"/>
        <activity
            android:name=".presentation.view.activities.BuyActivity"
            android:exported="true"/>
        <activity
            android:name=".presentation.view.activities.DetailedStockActivity"
            android:exported="true"/>
        <activity
            android:name=".presentation.view.activities.LoginActivity"
            android:exported="true"/>
        <activity
            android:name=".presentation.view.activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>